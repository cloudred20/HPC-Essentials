# Bio-BASH-Essentials
Fundamental BASH shell scripts designed to perform essential, daily command-line tasks.  

* If you edit or download any BASH script on a Windows machine (e.g., using Notepad++), you must **run dos2unix scriptname.sh** before first execution on a UNIX/Linux system. This converts the line break format to the one readable by UNIX/Linux.
