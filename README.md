# Bio-BASH-Essentials
Fundamental BASH shell scripts designed to perform essential, daily command-line tasks.  

* Ensure scripts are runnable on UNIX/Linux by fixing line endings
`dos2unix scriptname.sh`
* Check the md5sum of all input files to confirm the data downloaded properly from the server
`md5sum -c <reference_checksum_file>`
